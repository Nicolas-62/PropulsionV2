<!DOCTYPE html>

<title>Hello ActusController!</title>

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>{{ tree[3].element.title }}</h1>
        {{ dump(tree[3]) }}

    {{ dump(tree[3].element.children) }}
    This friendly message is coming from:
    <ul>
        {{ include('frontoffice/article/article.html.twig', {tree : tree[3]}) }}
    </ul>
</div>
{% endblock %}

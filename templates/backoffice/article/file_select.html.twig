
{% block _Article_file_select_widget %}
{#    <img src="{{ asset( ea.entity.fields.getByProperty(form.vars.name).value) }}" class="img-fluid">#}
{#    {{ ea.entity.fields.getByProperty(form.vars.name).formattedValue|trans|raw }}#}

{#    {{ dump(form.vars) }}#}
{#    {{  dump(ea.entity.fields.getByProperty(form.vars.name)) }}#}
    <div>
    <select id="custom-select" data-placeholder="{{ 'Choisissez un fichier..' }}"name="{{ form.vars.full_name }}" multiple="multiple">

        {% for choice in form.vars.data %}
            <option  value="{{ choice.id }}">
                {{ getName(choice.media) }}
            </option>
        {% endfor %}
    </select>
    </div>
{% endblock %}